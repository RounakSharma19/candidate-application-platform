export { JobsListing } from "./JobsListing";

export type TExperienceLevel = {
  minExp: number;
  maxExp: number;
  label: string;
};

export type TRole = {
  jobRole: string;
};

export type TMinBasePay = {
  minJdSalary: number;
};
